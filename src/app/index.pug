doctype html

html 
    head
        link(href="style/main.css" rel="stylesheet")
        script(src="https://cdn.jsdelivr.net/npm/vue@2")

    body
        #app
            .all_course
                template(v-for="course in all_course")
                    .course(v-bind:class="{ 'now-select': course.NowSelect }")
                        p {{ course.name }}
            .all_announce
                template(v-for="announce in all_announce")
    script.
        //- var webFrame = require('electron').webFrame;
        //- webFrame.setZoomLevelLimits(1, 1);

        var vue = new Vue({
            el: "#app",
            data: {
                all_course: [
                    {
                        NowSelect: true,
                        name: "Oni"
                    },
                    {
                        NowSelect: false,
                        name: "Hard"
                    }
                ],
                all_announce: [
                    
                ]
            }
        });
